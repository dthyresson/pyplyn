# Migration `20200921010244-migration`

This migration has been generated by A. David Thyresson at 9/20/2020, 9:02:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."StreamSource" AS ENUM ('FEEDLY')

CREATE TYPE "public"."StreamType" AS ENUM ('BOARD', 'PRIORITY')

CREATE TABLE "public"."EntryStream" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"lastAccessedAt" timestamp(3)   NOT NULL ,
"streamSource" "StreamSource"  NOT NULL ,
"streamType" "StreamType"  NOT NULL ,
"name" text   NOT NULL ,
"description" text   NOT NULL ,
"streamIdentifier" text   NOT NULL ,
"continuation" text   NOT NULL ,
PRIMARY KEY ("id")
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200921004625-migration..20200921010244-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -69,8 +69,30 @@
   rdfTypes  String[]
   sentiment Float    @default(0)
 }
+enum StreamSource {
+  FEEDLY
+}
+
+enum StreamType {
+  BOARD
+  PRIORITY
+}
+
+model EntryStream {
+  id               String       @id @default(cuid())
+  createdAt        DateTime     @default(now())
+  updatedAt        DateTime     @default(now())
+  lastAccessedAt   DateTime
+  streamSource     StreamSource
+  streamType       StreamType
+  name             String
+  description      String
+  streamIdentifier String
+  continuation     String
+}
+
 model Tweet {
   id              String          @id @default(cuid())
   createdAt       DateTime        @default(now())
   updatedAt       DateTime        @default(now())
```


