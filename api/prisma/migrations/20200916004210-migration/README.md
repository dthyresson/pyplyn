# Migration `20200916004210-migration`

This migration has been generated by A. David Thyresson at 9/15/2020, 8:42:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200915130237-migration..20200916004210-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -26,18 +26,18 @@
   article      Article?
 }
 model Article {
-  id             String          @id @default(cuid())
-  createdAt      DateTime        @default(now())
-  updatedAt      DateTime        @default(now())
+  id             String         @id @default(cuid())
+  createdAt      DateTime       @default(now())
+  updatedAt      DateTime       @default(now())
   publishedAt    DateTime
-  entry          Entry           @relation(fields: [entryId], references: [id])
+  entry          Entry          @relation(fields: [entryId], references: [id])
   entryId        String
   author         String
   title          String
-  url            String          @unique
-  ArticleContext ArticleContext?
+  url            String         @unique
+  ArticleContext ArticleContext
 }
 model ArticleContext {
   id        String   @id @default(cuid())
@@ -58,10 +58,10 @@
   author        String
   title         String
   content       String
   url           String          @unique
-  TweetContext  TweetContext?
-  TweetCategory TweetCategory[]
+  tweetContext  TweetContext?
+  tweetCategory TweetCategory[]
 }
 model TweetCategory {
   id        String   @id @default(cuid())
```


