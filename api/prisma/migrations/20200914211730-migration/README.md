# Migration `20200914211730-migration`

This migration has been generated by A. David Thyresson at 9/14/2020, 5:17:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Article" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"publishedAt" timestamp(3)   NOT NULL ,
"entryId" text   NOT NULL ,
"author" text   NOT NULL ,
"title" text   NOT NULL ,
"url" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Article.url_unique" ON "public"."Article"("url")

CREATE UNIQUE INDEX "Article_entryId_unique" ON "public"."Article"("entryId")

CREATE UNIQUE INDEX "Tweet.url_unique" ON "public"."Tweet"("url")

ALTER TABLE "public"."Article" ADD FOREIGN KEY ("entryId")REFERENCES "public"."Entry"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200913143627-migration..20200914211730-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -16,10 +16,23 @@
   updatedAt  DateTime @default(now())
   documentId String   @unique
   document   Json
   tweet      Tweet?
+  article    Article?
 }
+model Article {
+  id          String   @id @default(cuid())
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @default(now())
+  publishedAt DateTime
+  entry       Entry    @relation(fields: [entryId], references: [id])
+  entryId     String
+  author      String
+  title       String
+  url         String   @unique
+}
+
 model Tweet {
   id          String   @id @default(cuid())
   createdAt   DateTime @default(now())
   updatedAt   DateTime @default(now())
@@ -27,6 +40,6 @@
   entry       Entry    @relation(fields: [entryId], references: [id])
   entryId     String
   author      String
   title       String
-  url         String
+  url         String   @unique
 }
```


