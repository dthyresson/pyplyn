# Migration `20200914215136-migration`

This migration has been generated by A. David Thyresson at 9/14/2020, 5:51:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Entry" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"documentId" text   NOT NULL ,
"document" jsonb   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Article" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"publishedAt" timestamp(3)   NOT NULL ,
"entryId" text   NOT NULL ,
"author" text   NOT NULL ,
"title" text   NOT NULL ,
"url" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Tweet" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"publishedAt" timestamp(3)   NOT NULL ,
"entryId" text   NOT NULL ,
"author" text   NOT NULL ,
"title" text   NOT NULL ,
"url" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."TweetTextAnalysis" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"nlp" jsonb   NOT NULL ,
"tweetId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Entry.documentId_unique" ON "public"."Entry"("documentId")

CREATE UNIQUE INDEX "Article.url_unique" ON "public"."Article"("url")

CREATE UNIQUE INDEX "Article_entryId_unique" ON "public"."Article"("entryId")

CREATE UNIQUE INDEX "Tweet.url_unique" ON "public"."Tweet"("url")

CREATE UNIQUE INDEX "Tweet_entryId_unique" ON "public"."Tweet"("entryId")

ALTER TABLE "public"."Article" ADD FOREIGN KEY ("entryId")REFERENCES "public"."Entry"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Tweet" ADD FOREIGN KEY ("entryId")REFERENCES "public"."Entry"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."TweetTextAnalysis" ADD FOREIGN KEY ("tweetId")REFERENCES "public"."Tweet"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200914211730-migration..20200914215136-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -32,14 +32,24 @@
   url         String   @unique
 }
 model Tweet {
-  id          String   @id @default(cuid())
-  createdAt   DateTime @default(now())
-  updatedAt   DateTime @default(now())
-  publishedAt DateTime
-  entry       Entry    @relation(fields: [entryId], references: [id])
-  entryId     String
-  author      String
-  title       String
-  url         String   @unique
+  id                String              @id @default(cuid())
+  createdAt         DateTime            @default(now())
+  updatedAt         DateTime            @default(now())
+  publishedAt       DateTime
+  entry             Entry               @relation(fields: [entryId], references: [id])
+  entryId           String
+  author            String
+  title             String
+  url               String              @unique
+  TweetTextAnalysis TweetTextAnalysis[]
 }
+
+model TweetTextAnalysis {
+  id        String   @id @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  tweet     Tweet    @relation(fields: [tweetId], references: [id])
+  nlp       Json
+  tweetId   String
+}
```


